{
  "name": "projectos",
  "version": "0.0.0",
  "private": true,
  "description": "ProjectOS — CLI-first Autonomous Development Operating System (monorepo root)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/shared",
    "packages/core",
    "packages/database",
    "packages/workspace",
    "packages/orchestrator",
    "packages/scheduler",
    "packages/builders",
    "packages/verification",
    "packages/context",
    "packages/providers",
    "packages/reporting",
    "apps/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --workspaces --if-present",
    "projectos": "tsx apps/cli/src/index.ts",
    "projectos:doctor": "tsx apps/cli/src/index.ts doctor"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.19.1"
  }
}
